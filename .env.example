# Supabase — Project Settings → API
# The site renders fine without these: the catalog shows its empty state and
# the admin panel explains what is missing.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
